/// <reference types="node" />
import { Base } from "./Base";
import type { Koreanbots } from "../client/Koreanbots";
import type { WidgetOptions, WidgetTarget, WidgetType, WidgetData } from "../utils/types";
export declare class Widget extends Base {
    readonly koreanbots: Koreanbots;
    buffer: Buffer;
    readonly target: WidgetTarget;
    readonly type: WidgetType;
    readonly id: string;
    constructor(koreanbots: Koreanbots, buffer: Buffer, data: WidgetData);
    fetch(options?: WidgetOptions): Promise<Widget>;
}
