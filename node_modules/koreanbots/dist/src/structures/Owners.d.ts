import { Collection } from "discord.js";
import { User } from "./User";
import type { Koreanbots } from "../client/Koreanbots";
import type { RawUserInstance } from "../utils/types";
export declare class Owners extends Collection<string, User> {
    readonly koreanbots: Koreanbots;
    private data?;
    constructor(koreanbots: Koreanbots, data?: User[] | undefined);
    fetch(): Promise<(RawUserInstance | null)[]>;
}
