import { Base } from "./Base";
import { Owners } from "./Owners";
import { Discord } from "./Discord";
import type { RawBotInstance, BotFlags, Nullable, Category, BotState, BotStatus } from "../utils/types";
import type { Koreanbots } from "../client/Koreanbots";
export declare class Bot extends Base {
    readonly koreanbots: Koreanbots;
    id: string;
    name: string;
    tag: string;
    discriminator: string;
    avatar: Nullable<string>;
    owners: Owners;
    flags: BotFlags;
    lib: string;
    prefix: string;
    votes: number;
    servers: number;
    intro: string;
    desc: string;
    web: Nullable<string>;
    git: Nullable<string>;
    url: Nullable<string>;
    discord: Nullable<Discord>;
    category: Category[];
    vanity: Nullable<string>;
    bg: Nullable<string>;
    banner: Nullable<string>;
    status: Nullable<BotStatus>;
    state: BotState;
    shards: Nullable<number>;
    constructor(koreanbots: Koreanbots, data: RawBotInstance);
    fetchVotes({ cache }?: {
        cache: boolean;
    }): Promise<number>;
}
