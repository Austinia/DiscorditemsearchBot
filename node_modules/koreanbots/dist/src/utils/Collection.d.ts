/// <reference types="node" />
import { Collection } from "discord.js";
export interface LifetimeCollectionOptions {
    maxAge?: number;
    max?: number;
}
declare class LifetimeCollection<K, V> extends Collection<K, V> {
    readonly options?: LifetimeCollectionOptions | undefined;
    max?: number;
    maxAge?: number;
    readonly timeouts: Map<K, NodeJS.Timeout>;
    constructor(options?: LifetimeCollectionOptions | undefined);
    set(key: K, value: V): this;
    sweep(fn: (value: V, key: K, collection: this) => boolean, thisArg?: unknown): number;
    delete(key: K): boolean;
    clear(): void;
}
export default LifetimeCollection;
