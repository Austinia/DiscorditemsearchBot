import type { Version } from "../utils/types";
/**
 * API 버전에 따라 기본 라우트를 만듭니다
 */
export declare function getVersionRoute(version?: Version, unstable?: boolean): string;
export declare const getGlobalRoute: (unstable?: boolean) => string;
