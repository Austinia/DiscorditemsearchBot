import APIClient from "./RequestClient";
import type { RequestClientOptions, InternalOptions } from "../utils/types";
declare type Proxy = {
    client: APIClient;
    <A>(routeOptions?: InternalOptions): A;
};
declare function buildRoute(options: RequestClientOptions): Proxy;
export default buildRoute;
